syntax = "proto3";

message Map {
  map<int32, string> int_to_string = 1;
  map<string, float> string_to_float = 1234;

  message Mixed {
    repeated uint64 value = 1;
    string str = 2;
  }
  map<string, Mixed> string_to_mix = 12;
};
